/* eslint-disable */

describe('Blue button', () => {
    it('Changes color to blue', () => {
        cy.visit('http://localhost:3000/')
    })
})

describe('Red button', () => {
    it('Change color to red', () => {
        cy.visit('http://localhost:3000/')
    })
})